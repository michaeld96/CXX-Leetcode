# Leetcode C++ Debugging Suite

A Leetcode repo that contains all solve problems and a debug setup so that someone can
set through the implementation.

## Build

```
# Build source files.
cmake -S . -B build
# Run current problem.
make -C build <problem-number>
```